package com.tmobile.selenium.sam.action.types;

/**
 * The Enum ActionType.
 *
 * @author Rob Graff
 */
public enum ActionType {

	/** The Action. */
	Action, /** The Go. */
 Go, /** The Click. */
 Click, /** The Cookie. */
 Cookie, /** The Send. */
 Send, /** The Read. */
 Read, /** The Validate. */
 Validate, /** The Alert. */
 Alert, /** The Frame. */
 Frame, /** The Switch default. */
 SwitchDefault, /** The Window. */
 Window, /** The Condition. */
 Condition, /** The Key. */
 Key, /** The Move. */
 Move, /** The Select. */
 Select, /** The Go to step. */
 GoToStep, /** The Navigate. */
 Navigate, /** The Wait. */
 Wait
	
}
