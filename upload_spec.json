{
  "files":[
    {
      "pattern": "target/*.${BUILD_TYPE}",
      "target": "${ARTIFACT_REPO}/${GROUP_ID}/${ARTIFACT_ID}/${APP_VERSION}-${ARTIFACT_STAGE}/",
      "recursive": "true"
    },
    {
      "pattern": "target/*pom.xml",
      "target": "${ARTIFACT_REPO}/${GROUP_ID}/${ARTIFACT_ID}/${APP_VERSION}-${ARTIFACT_STAGE}/",
      "recursive": "false"
    }

  ]
}
