package com.tmobile.ct.codeless.ui.model.controls;

/**
 * The Enum ByTypes.
 *
 * @author Rob Graff
 */
public enum ByTypes {

	/** The id. */
	id, /** The css. */
 css, /** The tagname. */
 tagname, /** The linktext. */
 linktext, /** The partiallinktext. */
 partiallinktext, /** The xpath. */
 xpath, /** The name. */
 name, /** The classname. */
 classname

}
