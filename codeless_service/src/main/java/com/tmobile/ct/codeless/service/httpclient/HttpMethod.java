package com.tmobile.ct.codeless.service.httpclient;

/**
 * The Enum HttpMethod.
 *
 * @author Rob Graff
 */
public enum HttpMethod {

	/** The post. */
	POST, /** The put. */
 PUT, /** The get. */
 GET, /** The patch. */
 PATCH, /** The delete. */
 DELETE, /** The head. */
 HEAD, /** The options. */
 OPTIONS;
}
